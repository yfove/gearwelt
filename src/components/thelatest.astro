---
const articles = [
{
thumbnail: '/images/rolex.jpg',
category: 'Watches',
timeAgo: '20 mins ago',
title: 'Rolex’s Most Extreme Dive Watch Just Got Some Unexpected Company',
href: '/watches/rolex-extreme-dive-watch',
},
{
thumbnail: '/images/tesla.jpg',
category: 'Motoring',
timeAgo: '3 hours ago',
title: 'Tesla Just Launched a Car With a Built-In Drone',
href: '/motoring/tesla-drone-car',
},
];
---
<section class="latest-section max-w-4xl mx-auto px-4 py-6">
    <h2 class="text-xl font-semibold mb-4">The Latest</h2>
    <ul class="space-y-4">
        {articles.map(article => (
        <li class="flex items-start space-x-4">
            <img src={article.thumbnail} alt="" class="w-24 h-16 object-cover flex-shrink-0 rounded-sm" />
            <div class="flex-1">
                <div class="flex items-center text-xs text-gray-500 space-x-2">
                    <span class="uppercase">{article.category}</span>
                    <span>·</span>
                    <span>{article.timeAgo}</span>
                </div>
                <a href={article.href} class="block text-sm font-medium hover:text-amber-600">
                    {article.title}
                </a>
            </div>
        </li>
        ))}
    </ul>
</section>

<style>
    .latest-section {
        /* max width, padding via parent classes */
    }

    .latest-item img {
        @apply w-24 h-16 object-cover rounded-sm;
    }

    .latest-title {
        @apply text-sm font-medium hover:text-amber-600;
    }

    .meta-info {
        @apply text-xs text-gray-500 uppercase tracking-wide flex items-center space-x-1;
    }

    @media (max-width: 640px) {
        .latest-item img {
            @apply w-20 h-12;
        }

        .latest-title {
            @apply text-xs;
        }
    }
</style>